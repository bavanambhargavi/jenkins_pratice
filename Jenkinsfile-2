pipeline {  
    agent any  
    stages {  
            stage ('Build') { 
                steps{ 
                echo "Build this job" 
            }
            }  
            stage ('Test') {
                steps{
                echo "testing the job"
                }  
                 
            }  
            stage ('QA') { 
                steps{
                sh 'pip install pytest'
                } 
            }  
            stage ('Deploy') {
                steps{
                echo "Deployement"
                } 
            }  
            stage ('Monitor') { 
                steps{
                echo "Monitoring the job"
                } 
            }
    }
}  